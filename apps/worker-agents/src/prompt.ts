/**
 * Moved to @renkei/agents/step-prompts so the web app's "what the model
 * sees" surfaces render through the SAME code the engine sends — kept here
 * as a re-export so the engine's imports read as before.
 */

export * from '@renkei/agents/step-prompts';
