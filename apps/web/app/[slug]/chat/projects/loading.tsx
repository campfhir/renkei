import { FramedIndexSkeleton } from '../_components/frame-skeleton';

/** Projects: mine, then the ones shared with me. */
export default function ProjectsLoading() {
  return <FramedIndexSkeleton label="Loading projects…" />;
}
