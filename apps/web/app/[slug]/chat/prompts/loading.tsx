import { FramedIndexSkeleton } from '../_components/frame-skeleton';

/** Prompt libraries: mine, then the shared and published ones. */
export default function PromptLibrariesLoading() {
  return <FramedIndexSkeleton label="Loading prompt libraries…" />;
}
