# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...but the templates are the point: they hold no values, and they are how the
# next person learns which variables exist. The blanket rule above had them
# ignored, so the repository documented none of its configuration.
!.env.example
!.env*.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
dist/

# claude code worktrees
.claude/worktrees/

# pnpm content-addressable store. Local cache, never source: it was committed
# once by accident and cost 74MB across 28k files before being purged.
.pnpm-store/

# Playwright artifacts. An error-context dump once captured a live Azure AD
# client secret from a test, which GitHub push protection caught.
test-results/
playwright-report/
